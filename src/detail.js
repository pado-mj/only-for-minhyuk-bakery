const id=location.pathname.split('/').filter(Boolean).pop();const root=document.querySelector('#detail');const r=await fetch(`/api/cakes?id=${encodeURIComponent(id)}`);const d=await r.json();if(!r.ok){root.textContent='Cake not found';}else{const c=d.cake;const birthday=new Date(new Date().getFullYear()+'-11-02T15:00:00Z');const lit=Date.now()>=birthday.getTime();root.innerHTML=`<h1>CAKE #${String(c.public_number).padStart(4,'0')}</h1><div id="flame">${lit?'<div class="flame">🔥</div>':''}<img src="${c.final_image_url}" alt="cake"></div><h2>${c.nickname}${c.country?' · '+c.country:''}</h2><button id="open">${lit?'MAKE A WISH':'READ LETTER'}</button><div id="letter" class="letter" hidden>${c.letter}</div><div class="actions"><button id="save">SAVE IMAGE</button><button id="copy">COPY LINK</button></div>`;document.querySelector('#open').onclick=()=>{document.querySelector('.flame')?.remove();document.querySelector('#letter').hidden=false;fetch('/api/view',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({public_id:id})})};document.querySelector('#copy').onclick=async()=>{await navigator.clipboard.writeText(location.href);alert('링크를 복사했어요 ♡')};document.querySelector('#save').onclick=()=>{const a=document.createElement('a');a.href=c.final_image_url;a.download=`minhyuk-bakery-${c.public_number}.webp`;a.click()}}
