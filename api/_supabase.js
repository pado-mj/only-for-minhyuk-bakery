export const cfg=()=>({url:process.env.SUPABASE_URL,key:process.env.SUPABASE_SERVICE_ROLE_KEY});export async function sb(path,opt={}){const {url,key}=cfg();if(!url||!key)throw new Error('Missing Supabase environment variables');return fetch(`${url}/rest/v1/${path}`,{...opt,headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json',Prefer:opt.prefer||'',...(opt.headers||{})}})}
